#!/usr/bin/env node

/*thisis a comment, i'm going to use ome operators in it.
let's see how it went*/

let x = 5;
let y = 10;

let sum = (x + y);
let sub = (x - y);
let mul = (x * y);
let div = (x / y);
let mod = (x % y);

console.log ("sum is " + sum);
console.log ("sub is " + sub);
console.log ("mul is " + mul);
console.log ("div is " + div);
console.log ("mod is " + mod);
